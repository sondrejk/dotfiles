-- Browser-based preview is glitchy on zoom/scroll; using zathura + tmux
-- instead, bound to the same <leader>cp key (see config/autocmds.lua).
return {
  { "chomosuke/typst-preview.nvim", enabled = false },
}
